const country = 'Ukraine';
const continet = 'Europe';
const population = 43031753;

const isIsland = false;
let language;

console.log(country, population, isIsland, language);

language = 'Ukrainian';

const populationHalf = population / 2;
console.log(population++);
const finladPopulation = 6000000
console.log(population > finladPopulation);
console.log(population < 33000000);

const description = `${country} is in ${continet}, and its ${population} people speak ${language}`;
console.log(description);